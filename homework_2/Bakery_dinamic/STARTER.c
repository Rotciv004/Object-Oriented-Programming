/*#include "ui.h"
#include <crtdbg.h>

int main()
{
	Repo* repo = createRepo(50, freeMaterial);
	Services* services = createServices(repo);
	Ui* ui = createUi(services);

	startUi(ui);
	freeUi(ui);

	//char* mere = (char*)malloc(10 * sizeof(char));

	_CrtDumpMemoryLeaks();
	return 0;
}*/